{
    "version": 1,
    "formatters": {
        "default": {
            "format": "%(asctime)s | %(levelname)s | %(name)s | %(funcName)s | %(message)s"
        }
    },
    "handlers": {
        "serialfile": {
            "class": "logging.FileHandler",
            "formatter": "default",
            "filename": "codes/scripts/compare_1st.log"
        }
    },
    "loggers": {
        "fipy": {
            "level": "DEBUG",
            "handlers": ["serialfile"]
        }
    }
}
